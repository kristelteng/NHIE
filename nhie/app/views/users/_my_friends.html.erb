<% if @user == current_user %> 
<h1>My Friends</h1>
	<ul>
		<% @users.each do |user| %>
			<% unless current_user.friends?(user) %> 

				<li>
				<%= link_to user.first_name, user_path(user) %>
				</li>
			<% end %>
		<% end %>
	</ul>
<% end %>