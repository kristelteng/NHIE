<h1><%= @user.first_name %></h1>
  <%# unless @user == current_user || @user.request_friendship!(@user) || current_user.friends.include?(@user) || current_user.pending_friends.include?(@user) %>
    <%# form_for(:friendship, :url => user_friends_path(:user_id => current_user.id, :friend_id => @user.id)) do |f| %>
      <%= submit_tag "Add to My Friends" %>
    <%# end %>
  <%# end %>
<%# end %>

<div class="my-friends">
	<%= render :partial => 'my_friends' %>
</div>